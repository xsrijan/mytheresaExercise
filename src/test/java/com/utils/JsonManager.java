package com.utils;

import java.io.FileNotFoundException;
import java.io.IOException;

import org.json.simple.parser.ParseException;

//    Class c is generated using http://www.jsonschema2pojo.org/

public class JsonManager {


public void readJSONData() throws FileNotFoundException, IOException, ParseException
{
	/*
	 * JSONParser parser = new JSONParser();
	 * 
	 * //Object obj = parser.parse(new
	 * FileReader("src/test/resources/PlainText/DPCTEST/procureExpirePlan1.json"));
	 * 
	 * System.out.println("File found"); // A JSON object. Key value pairs are
	 * unordered. JSONObject supports java.util.Map interface. //JSONObject
	 * jsonObject = (JSONObject) obj;
	 * 
	 * // A JSON array. JSONObject supports java.util.List interface. //Object
	 * userId = jsonObject.get("username"); //Object pwd =
	 * jsonObject.get("password"); //
	 * user.setUsername(jsonObject.get("username").toString()); //
	 * user.setPassword((jsonObject.get("password").toString()));
	 * 
	 * //return user; // System.out.println("id"+userId.toString());
	 */
}}
